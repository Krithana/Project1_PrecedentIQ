# PrecedentIQ — verified requirements (checked against PyPI, Sept 2026)
# Install with: pip install -r requirements.txt

# ---- Data acquisition ----
boto3==1.43.87            # AWS S3 access (Open Data Registry, no account needed)
datasets==5.0.1           # Hugging Face datasets (IL-TUR, LawSum, ILDC)
huggingface-hub==1.29.0

# ---- Document parsing ----
pymupdf==1.28.2            # PDF text extraction (import as `pymupdf`, not `fitz`)
pdfplumber==0.11.10        # PDF table/layout parsing

# ---- Embeddings & retrieval ----
sentence-transformers==6.0.1
transformers==5.16.1
torch==2.14.0               # CPU build note: see README setup step
qdrant-client==1.19.0
rank-bm25==0.2.2

# ---- LLM orchestration ----
langchain==1.3.18
langchain-community==0.4.2
langgraph==1.2.11
ollama==0.6.2

# ---- Backend / app ----
fastapi==0.141.1
uvicorn==0.52.4
streamlit==1.63.0
pydantic==2.13.5

# ---- Export ----
python-docx==1.2.0

# ---- Utilities ----
tqdm==4.70.0
python-dotenv==1.2.3
pyyaml==6.0.3
